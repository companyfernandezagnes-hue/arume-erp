{
  "name": "arume-erp-tests",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "jest --coverage",
    "test:watch": "jest --watch",
    "lint": "eslint . --ext .js"
  },
  "devDependencies": {
    "jest": "^29.6.0",
    "eslint": "^8.0.0"
  }
}